export const SkillBadge = ({ value }: { value: string }) => (
  <span className="inline-flex items-center justify-center px-4 py-1 mt-1 mr-2 font-bold text-md text-white bg-blue-600 rounded-full">
    {value}&nbsp;
  </span>
);
